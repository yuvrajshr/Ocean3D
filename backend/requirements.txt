# INCOIS 3D Ocean Data Visualization - backend

fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27

# NetCDF / data handling
xarray>=2024.9
netCDF4>=1.7
numpy>=2.0
pandas>=2.2

# Copernicus Marine. Optional: without credentials in backend/.env those layers
# are just hidden.
copernicusmarine>=2.4

# Assistant (Gemini). Optional: without GEMINI_API_KEY the assistant is hidden.
google-genai>=2.22
